# Streamlit app runtime dependencies
streamlit>=1.28.0
pandas>=2.0.0
numpy>=1.24.0
joblib>=1.3.0
shap>=0.42.0
matplotlib>=3.7.0
plotly>=5.15.0
scikit-learn>=1.3.0

# The model's own library — whichever won the tournament.
# Uncomment the one that matches your saved pipeline:


catboost>=1.2.0

# Only needed if the winning strategy was SMOTE or ADASYN
# (the pipeline pickle references imblearn classes)
imbalanced-learn>=0.11.0
