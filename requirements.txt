# ─────────────────────────────────────────────────────────────────────────────
# Streamlit app runtime dependencies
# Versions pinned to match the Colab training environment exactly.
# The saved pipeline (.joblib) was created with these versions — mismatches
# will cause pickle deserialization errors on deployment.
# If you retrain the model on a new Colab runtime, re-run the version-check
# cell in the notebook and update the pins below accordingly.
# ─────────────────────────────────────────────────────────────────────────────

streamlit>=1.28.0

# Core ML — must match Colab training environment exactly
scikit-learn==1.6.1
imbalanced-learn==0.14.1
joblib==1.5.3

# Model library — CatBoost won the tournament
catboost==1.2.10

# Explainability
shap==0.51.0

# Data
pandas==2.2.2
numpy==2.0.2

# Visualisation
matplotlib==3.10.0
plotly==5.24.1
